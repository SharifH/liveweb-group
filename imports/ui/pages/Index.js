import React from 'react';
import TokboxChat from './tokbox/tokbox-container'; // we pull the container in from the container file instead of the actual component

const Index = () => (
  <div className="Index">
     <TokboxChat />
  </div>
);
